// Pequena interação: mensagem automática ao clicar no botão

const btn = document.querySelector('.btn');

btn.addEventListener('click', () => {
  console.log("Cliente clicou em pedir orçamento");
});